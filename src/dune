(library
  (name tnqcc)
  (libraries core base str)
  (preprocess
    (pps
     ppx_deriving.show
     ppx_deriving.eq
     ppx_deriving.ord
     ppx_sexp_conv))
  (synopsis "Trox's not-quite-C compiler")
  )
